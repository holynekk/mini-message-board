extends layout

block content
  header.head-head
    h1= title
    p.only-text Dude Just Leave Me Some Notes
  main.mama
    a.add-message(href='/new') Add New Message
    each message in messages
      div.mess
        h2= message.title
        h3= message.user
        h4= message.text
        h5= message.day

